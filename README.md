#  Random Number Generator

## To run do the following

* Clone the repo
* Change into repo directory
* Change the number in numbers.js if necessary
* Open the index.html file in a browser and start generating numbers